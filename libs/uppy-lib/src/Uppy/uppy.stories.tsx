import type { FunctionComponent } from 'react';
import React from 'react';

import { UpppyButton } from '.';

export const UpppyButtonStories: FunctionComponent = () => (
  <UpppyButton getAuth={() => ''} uploadEndpoint=""></UpppyButton>
);
